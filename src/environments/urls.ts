import routing from "./routing";

const urls ={
  home: "/" + routing.home,
  contact: "/" + routing.contact,
  login: "/" + routing.login,
  register: "/" + routing.register,
  settings: "/" + routing.settings,
  dashboard: "/" + routing.dashboard,
  recipe: "/" + routing.recipe,
  new_recipe: "/" + routing.new_recipe,
  payment_method: "/" + routing.payment_method,
  recipe_type: "/" + routing.recipe_type,
  recipe_raport: "/" + routing.recipe_raport,
}

export default urls;
